import KanbanBoard from "@/components/KanbanBoard";

export const dynamic = "force-dynamic";

/**
 * Home Page — renders the Kanban board as the main application view.
 */
export default function Home() {
  return <KanbanBoard />;
}
